# Generated output. Re-creatable with `python build/build.py`.
# Force-add the built file (git add -f dist/skills-library.html) if you want to version a release.
dist/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS and editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Local agent scaffolding
.agents/
.codex/
.claude/worktrees/

# Harvest sources — upstream repos kept locally as read-only specifications to decompose, not vendor
agent-skills/
# AGENTS.md is tracked: it is the Codex-facing companion to CLAUDE.md.

# Secrets and local environment
.env*
*.local
credentials.json
token.json
oauth_creds.json
client_secret*.json
.ssh/
.mcp-auth/
